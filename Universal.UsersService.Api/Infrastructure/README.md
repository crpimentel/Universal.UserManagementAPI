# Infrastructure Layer
# Propósito: Implementa acceso a datos, integración con servicios externos, configuración de EF Core, BCrypt, JWT, etc.
# Ejemplo: UserRepository, configuración EF Core, servicios externos
